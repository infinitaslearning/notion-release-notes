name: Test Notion

on: 
    workflow_dispatch

jobs:
    notion: 
        runs-on: ubuntu-latest
        steps:
            - name: Notion release notes        
              uses: jlpdeveloper/notion-markdown-creator@8cd35d83a40e6930e220696de68eee4c4fd222c7
              with:          
                token: ${{ secrets.NOTION_TOKEN }}
                database: ${{ secrets.NOTION_DATABASE }}
                name: Super Amazing Service    
                filepath: 'test.md'