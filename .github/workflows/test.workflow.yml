name: Test Notion

on: 
    workflow_dispatch

jobs:
    notion: 
        runs-on: ubuntu-latest
        steps:
            - name: Notion release notes        
              uses: jlpdeveloper/notion-markdown-creator@af63b28b4a741d1c4f645d417d855e076ae4d14c
              with:          
                token: ${{ secrets.NOTION_TOKEN }}
                database: ${{ secrets.NOTION_DATABASE }}
                name: Super Amazing Service    
                filepath: 'test.md'